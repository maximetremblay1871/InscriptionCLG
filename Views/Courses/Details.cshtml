@model Models.Course
@{
    ViewBag.PageTitle = "Cours - Détails";
}

<div id="detailsPanel" class="detailsPanel">
    <!-- auto refreshed -->
</div>

@section Scripts {
    <script>
        $(() => {
            newSession(600);
            courseDetails_ARP = new AutoRefreshedPanel("detailsPanel", "/Courses/GetCourseDetails", 5, mediaDetailsPostRefresh);
        })

        function mediaDetailsPostRefresh() {
           // todo
        }
    </script>
}